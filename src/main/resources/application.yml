spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: jdbc:mysql://${FREAM_DB_URL:localhost}:${FREAM_DB_PORT:3306}/${FREAM_DB_SCHEME:fream}?useSSL=false
    username: ${FREAM_DB_USERNAME}
    password: ${FREAM_DB_PASSWORD}
